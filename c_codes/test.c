#include <stdio.h>
#include <stdbool.h>
int main() {
  int numeros[5];
  int i;
  for (i = 0; i < 5; i++) {
    printf("Escolha um núumero\n");
    scanf("%d", &numeros[i]);
  }
  foreach()
  return 0;
}
